name: e2e_smoke_tests
on:
  workflow_call:
    inputs:
      repo_name:
        required: true
        description: The name of the github repository
        default: ${{ github.repository }}
        type: string
      ref:
        required: true
        description: The git ref to use
        default: ${{ github.sha }}
        type: string
      cl_repo:
        required: true
        description: The chainlik ecr repository to use
        default: public.ecr.aws/z0b1w9r9/chainlink
        type: string
      cl_image:
        required: true
        description: The chainlink image to use
        default: develop
        type: string
    secrets:
      QA_AWS_ACCESS_KEY_ID:
        required: true
        description: The AWS access key id to use
      QA_AWS_SECRET_KEY:
        required: true
        description: The AWS secret key to use
      QA_AWS_REGION:
        required: true
        description: The AWS region to use
      QA_AWS_ROLE_TO_ASSUME:
        required: true
        description: The AWS role to assume
      QA_KUBECONFIG:
        required: true
        description: The kubernetes configuation to use
        
jobs:
  e2e_build_contracts:
    name: E2E Build Contracts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@f25a3a9f25bd5f4c5d77189cab02ff357b5aedeb # v2.4.1
        with:
          repository: ${{ inputs.repo_name }}
          ref: ${{ inputs.ref }}
      # We sadly can't reuse the build action since local reusable actions don't work with workflow_call
      - name: Cache cargo registry
        uses: actions/cache@v2.1.7
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-v2-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      - name: Cache cargo index
        uses: actions/cache@v2.1.7
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-v2-cargo-index-${{ hashFiles('**/Cargo.lock') }}
      - name: Build Artifacts
        shell: bash
        run: make contracts_compile
      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: artifacts
          path: artifacts
  e2e_run_smoke_tests:
    name: E2E Run Smoke Tests
    runs-on: ubuntu-latest
    needs: e2e_build_contracts
    env:
      CGO_ENABLED: 1
    steps:
      - uses: actions/checkout@f25a3a9f25bd5f4c5d77189cab02ff357b5aedeb # v2.4.1
        with:
          repository: ${{ inputs.repo_name }}
          ref: ${{ inputs.ref }}
      - name: Run Tests
        uses: smartcontractkit/ctf-ci-e2e-action@v1.0.0
        with:
          test_command_to_run: make test_smoke
          cl_repo: ${{ inputs.cl_repo }}
          cl_image_tag: ${{ inputs.cl_image }}
          build_guantlet_command: make build_js && make artifacts_cp_gauntlet
          download_artifacts_path: artifacts
          publish_report_paths: "./tests-smoke-report.xml"
          publish_check_name: Smoke Test Results
          QA_AWS_ACCESS_KEY_ID: ${{ secrets.QA_AWS_ACCESS_KEY_ID }}
          QA_AWS_SECRET_KEY: ${{ secrets.QA_AWS_SECRET_KEY }}
          QA_AWS_REGION: ${{ secrets.QA_AWS_REGION }}
          QA_AWS_ROLE_TO_ASSUME: ${{ secrets.QA_AWS_ROLE_TO_ASSUME }}
          QA_KUBECONFIG: ${{ secrets.QA_KUBECONFIG }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CGO_ENABLED: "1"
