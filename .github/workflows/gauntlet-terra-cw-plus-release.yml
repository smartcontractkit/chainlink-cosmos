name: gauntlet-terra-cw-plus-release

on:
  push:
    tags:
      - "gauntlet-terra-cw-plus-v[0-9]+.[0-9]+.[0-9]" # Push events to matching v*, i.e. gauntlet-terra-cw-plus-v1.0
      - "gauntlet-terra-cw-plus-v[0-9]+.[0-9]+.[0-9]+-rc*" # Push events to matching v*, i.e. gauntlet-terra-cw-plus-v1.0-rc1

jobs:
  release-gauntlet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@f25a3a9f25bd5f4c5d77189cab02ff357b5aedeb # v2.4.1
      - name: Build Gauntlet
        run: yarn && yarn bundle
      - name: Create Archive
        run: tar cfvz gauntlet-terra-cw-plus.tar.gz packages-ts/gauntlet-terra-cw-plus/**
      - name: Release Gauntlet
        uses: softprops/action-gh-release@v1
        with:
          files: |
            gauntlet-terra-cw-plus.tar.gz
            bin/gauntlet-terra-*
